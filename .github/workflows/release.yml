name: release

on:
  push:
    tags:
      - v*
  workflow_dispatch:  

permissions:
  contents: write

jobs:
  release:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
          with:
            fetch-depth: 0

        - name: Create Release
          uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3
          if: startsWith(github.ref, 'refs/tags/')
          with:
            tag_name: ${{ github.ref_name }}
            name: Release ${{ github.ref_name }}
            draft: true
            prerelease: false
            generate_release_notes: true
  artefact:
    strategy:
          matrix:
            include:
              - name: Linux x86_64
                os: ubuntu-latest
                target: x86_64-unknown-linux-musl
                suffix: ''
    runs-on: ${{ matrix.os }}
    name: Create artefact for ${{ matrix.name }}
    needs: [release]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - name: Stable Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          target: ${{ matrix.target }}
          toolchain: stable
      - name: Install musl-tools
        run: sudo apt update -y && sudo apt install musl-tools -y
        if: matrix.os == 'ubuntu-latest'
      - name: Build
        run: cargo build --target ${{ matrix.target }} --release
      - name: Move binary
        run: mv target/${{ matrix.target }}/release/richard${{ matrix.suffix }} richard-${{ github.ref_name }}-${{ matrix.target }}${{ matrix.suffix }}
      - name: Upload Artefact to release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3
        if: startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: true
          prerelease: false
          files: richard-${{ github.ref_name }}-${{ matrix.target }}${{ matrix.suffix }}